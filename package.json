{
  "name": "server",
  "version": "0.0.1",
  "description": "IDK yet",
  "main": "dist/index.js",
  "scripts": {
    "build": "yarn clean && tsc",
    "serve": "yarn clean && tsc && node dist",
    "start": "tsc --watch & nodemon dist",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "node",
    "fastify",
    "test"
  ],
  "author": "DONT@MEBRO",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^23.1.6",
    "fastify": "^1.8.0",
    "tslint-config-airbnb": "^5.9.2",
    "typescript": "^2.9.2"
  },
  "devDependencies": {
    "jest": "^23.4.1",
    "nodemon": "^1.18.2",
    "tslint": "^5.18.0"
  }
}
